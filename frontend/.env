# Frontend Environment Configuration
# Single base URL for all API calls - routes through backend reverse proxy

# API Base URL - all requests go through this path (never call Flask directly)
REACT_APP_API_BASE=/api

# Development server port
PORT=3000

# Development mode flag
NODE_ENV=development

# Disable source map warnings in development
GENERATE_SOURCEMAP=false

# React app title
REACT_APP_TITLE=CodexDominion.app

# API timeout settings
REACT_APP_API_TIMEOUT=30000

# WebSocket configuration
REACT_APP_WS_ENABLED=true